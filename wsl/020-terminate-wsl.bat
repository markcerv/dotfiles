echo Run this from the command prompt:
echo wsl -l --all
echo _
echo _
echo Now terminate one of the WSL's, so that the
echo /etc/wsl.conf file will get reprocessed
echo _
echo wsl -t Ubuntu-20.04   # use the correct distro name